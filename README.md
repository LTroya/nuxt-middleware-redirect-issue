# nuxt-redirects-issue

### Steps to reproduce

After cloning the repo:

```
yarn
yarn generate && yarn start
```

Go to `/dummy` directly on the browser. It should redirects you to the home as when you click on the `dummy` link on the navbar.
